import styles from './emptyCommentaries.module.scss';

const EmptyCommentaries = () => {
    return (
        <div className={styles.main}>
            <h3>Commentaries is empty</h3>
        </div>
    );
};

export default EmptyCommentaries;
